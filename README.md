# ask-panda-toolkit
To be written


cd /Users/nilsnilsson/Development/mcp/ask-panda-toolkit
export PYTHONPATH=$PWD
streamlit run interfaces/streamlit/chat.py

Start MCP server (stdio)

From repo root:
```bash
python3 -m askpanda_mcp.server
```

Start Streamlit:

From repo root:
```bash
export PYTHONPATH=$PWD
streamlit run interfaces/streamlit/chat.py
```